const SignUp = () => {
  return (
    <>
      <button onClick={() => {}} className="signup">
        Create New Account
      </button>
      <br></br>
      <button onClick={() => {}} className="signup">
        Sign In
      </button>
    </>
  );
};

export default SignUp;
