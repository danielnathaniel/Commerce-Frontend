import styled, { ThemeProvider } from "styled-components";
import { createGlobalStyle } from "styled-components";
// what is ThemeProvider css?
const CartStyle = createGlobalStyle`

`;
export default CartStyle;
